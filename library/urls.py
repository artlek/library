from django.urls import path, reverse_lazy
from . import views
from library.views import (
    RegistrationView,
    LibrarianRegistrationView,
    LoginView, 
    BooksView, 
    BookDetailView, 
    AddBookView, 
    EditBookView, 
    DeleteBookView,
    IndexView,
    BorrowBookView,
    NewReturnView,
    BorrowingsView,
    ProfileView,
    LibrarianDashboardView,
    ReaderDashboardView,
    AuthorsView,
    CategoriesView,
    AddAuthorView,
    EditAuthorView,
    DeleteAuthorView,
    PublishersView,
    AddPublisherView,
    EditPublisherView,
    DeletePublisherView,
    CoversView,
    AddCoverView,
    EditCoverView,
    DeleteCoverView,
    AddCategoryView,
    EditCategoryView,
    DeleteCategoryView,
    ReadersView,
    BookViewApi,
    BookCatalogueView,
    )
from library import views
from rest_framework.urlpatterns import format_suffix_patterns


urlpatterns = [
    path("", IndexView.as_view(), name="index"),
    path('login/', LoginView.as_view(), name='login'),
    path("logout", views.logout_view, name="logout"),
    path('books', BooksView.as_view(), name='books'),
    path('covers', CoversView.as_view(), name='covers'),
    path('categories', CategoriesView.as_view(), name='categories'),
    path("authors", AuthorsView.as_view(), name="authors"),
    path("publishers", PublishersView.as_view(), name="publishers"),
    path('add-book', AddBookView.as_view(), name='add_book'),
    path('add-author', AddAuthorView.as_view(), name='add_author'),
    path('add-publisher', AddPublisherView.as_view(), name='add_publisher'),
    path('add-category', AddCategoryView.as_view(), name='add_category'),
    path('add-cover', AddCoverView.as_view(), name='add_cover'),
    path("registration/", RegistrationView.as_view(), name="registration"),
    path("librarian-registration/", LibrarianRegistrationView.as_view(), name="librarian-registration"),
    path("book-detail/<slug:pk>/", BookDetailView.as_view(), name="book_detail"),
    path("edit-book/<slug:pk>/", EditBookView.as_view(), name="edit_book"),
    path("edit-author/<slug:pk>/", EditAuthorView.as_view(), name="edit_author"),
    path("edit-cover/<slug:pk>/", EditCoverView.as_view(), name="edit_cover"),
    path("edit-category/<slug:pk>/", EditCategoryView.as_view(), name="edit_category"),
    path("edit-publisher/<slug:pk>/", EditPublisherView.as_view(), name="edit_publisher"),
    path("delete-book/<slug:pk>/", DeleteBookView.as_view(), name="delete_book"),
    path("delete-author/<slug:pk>/", DeleteAuthorView.as_view(), name="delete_author"),
    path("delete-cover/<slug:pk>/", DeleteCoverView.as_view(), name="delete_cover"),
    path("delete-category/<slug:pk>/", DeleteCategoryView.as_view(), name="delete_category"),
    path("delete-publisher/<slug:pk>/", DeletePublisherView.as_view(), name="delete_publisher"),
    path("borrow-book", BorrowBookView.as_view(), name="borrow_book"),
    path("return-book", NewReturnView.as_view(), name="return_book"),
    path("borrowings", BorrowingsView.as_view(), name="borrowings"),
    path("profile", ProfileView.as_view(), name="profile"),
    path("librarian/dashboard", LibrarianDashboardView.as_view(), name="librarian_dashboard"),
    path("reader/dashboard", ReaderDashboardView.as_view(), name="reader_dashboard"),
    path("readers", ReadersView.as_view(), name="readers"),
    path('books-api/', BookViewApi.as_view(), name="books-api-list"),
    path("catalogue/", BookCatalogueView.as_view(), name="book-catalogue"),
]
